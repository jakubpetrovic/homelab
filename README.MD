NOTE: create ssh keypair wtih default name id_rsa in the project directory before running terraform apply or copy existing public key and rename it to ir_rsa.pub (alternatively change file name in cloud_init.cfg)